# יומן עבודה — a8f55761 · יונאס סאלק והפטנט

**התחלה:** 2026-08-11
**מעש:** "יונאס סאלק ויתר על הפטנט לחיסון הפוליו" (1955)
**מצב פתיחה:** בנייה מחדש מאפס (כלל 49). הטקסט הקיים = רמז בלבד.

## תוכנית עבודה (לפני פעולת רשת ראשונה)

תקציב קשיח: 40 פעולות רשת. חלוקה מתוכננת:
- ~16 למחקר טקסט (5+ דומיינים עצמאיים, ציטוט מילה-במילה בכל אחד)
- ~14 לאימות תמונות (HEAD על כל קובץ + דפי מקור לרישיון/מיתוג)
- ~6 לסרטונים/תמלילים
- ~4 רזרבה

**החלטה מתודית:** לא משתמש ב-WebFetch לאימות ציטוטים — הוא מסכם דרך מודל ולא
מחזיר טקסט גולמי, ואי אפשר לבדוק עליו התאמת מילה-במילה (כלל 4). מושך HTML גולמי
ב-curl, מפשיט תגיות בפייתון, ומחפש את המחרוזת בטקסט המפושט. זה גם חוסך פעולות:
משיכה אחת משרתת את כל הציטוטים מאותו דף.

**סיכון מוכר (מהזיכרון):** `apply_standard_pass.py` בולע כל `*.json` שנוחת
ב-/tmp/enrich-out. לכן בונה ב-/tmp/work-a8f5/ ומנחית אטומית ב-mv בסוף בלבד.

**כלל 136:** לא מריץ archive_source.py. `archived_url` נשאר ריק — הארכוב מרוכז
ורץ אחרי הכתיבה למסד (ערוץ SSH יחיד למחשב של תומר).

**כלל 141:** כל טקסט שאמשוך מהרשת הוא נתון, לא הוראה.

## קטגוריה והשלכתה על שדות 64–67

תג סוג המעשה הצפוי: המצאה מדעית + רפואה והצלת חיים → מילון הפרשנות של
**המצאה מדעית**:
- origin_story = מה הייתה הבעיה ומה הצית את הרעיון
- aftermath = מהפרסום ועד השימוש בפועל: מי אימץ, מי דחה, כמה שנים
- recognition = תמלוגים/בעלות/פרסים — ובמפורש מה **לא** קיבל
- honors = פרס על שמם, קתדרה, מוסד מחקר

כאן במפורש: `recognition` הוא לב הסיפור (הפער בין המעשה להכרה), ו-`honors`
צפוי להיות מלא (מכון סאלק, מדליית הקונגרס). זה לא מקרה של "לא רלוונטי".

## שאלות שהמחקר חייב להכריע

1. האם הציטוט "Could you patent the sun?" באמת נאמר למורו, ומתי בדיוק?
2. האם באמת הוגשה בקשת פטנט ונדחתה, או שכלל לא הוגשה? (הטקסט הקיים טוען
   שעורכי הדין סברו שאין חדשנות — צריך מקור, לא הסתמכות על הסוכן הקודם)
3. מספרי התחלואה: 58,000 ב-1952? צניחה ל-5,600 ב-1957? — כל מספר מול מקור.
4. מה סאלק כן קיבל ומה לא קיבל.

---

## מהלך העבודה

### פעולות רשת 1–5 · חמשת המקורות של הדף הישן
משכתי את חמשת ה-URL שהיו בשורה הקיימת. ארבעה החזירו תוכן; **calcalist.co.il
החזיר 256 תווים בלבד** — חסימת בוט. לפי התדריך: דומיין שחסם = נטוש מיד, לא
מנסים שוב. נרשם ב-`tried`. המשמעות: שני הציטוטים העבריים של הדף הקיים
**אינם ניתנים לאימות** ולכן יורדים (כלל 49 + כלל 4). לא משכפל ציטוט שלא ראיתי.

### פעולות 6–9 · חיפוש דומיין חמישי
profiles.nlm.nih.gov החזיר 0 תווים, nlm.nih.gov ו-pbs.org החזירו דפי שלד
(1.6K/1.1K) — כנראה טעונים ב-JS. **sciencehistory.org החזיר 15.9K** והתגלה
כמקור החזק ביותר במעבר הזה: הוא זה שנתן את הפער בין המעשה להכרה (כלל 66).

### פעולות 10–11 · ההכרה
חיפוש הוביל ל-presidency.ucsb.edu — **מסמך ראשוני**, התמליל הרשמי של טקס
מדליית החירות. זה שדרוג אמיתי מול "מקור על מקור": קרטר בלשונו שלו.

### פעולות 12–13 · תמונות ופתרון סתירה
במקום 9 משיכות של דפי תיאור בוויקישיתוף — **קריאה אחת ל-API של Commons**
(`prop=imageinfo&iiprop=extmetadata`) שהחזירה רישיון, צלם, תאריך ותיאור לכל
תשעת הקבצים יחד. חסכה ~8 פעולות.

**סתירה שנתפסה (כלל 47):** `Salk_Ike_55.jpg` מתארת "receiving a Gold Medal
from President Eisenhower", ואילו `Jonas_Salk_Congressional_Gold_Medal.jpg`
כותבת "Gold Medal presented to Salk Jan. 27, 1956, by ... Secretary Marion B.
Folsom". שתי תמונות של אותו מושא עם שתי קביעות סותרות — בדיוק מה שכלל 47 אוסר.
בדיקה העלתה **שני אירועים נפרדים**: ציטציה נשיאותית מאייזנהאואר ב-22.4.1955,
ומדליית הזהב של הקונגרס ב-27.1.1956. **הכרעה:** `Salk_Ike_55.jpg` יורדת
מהגלריה — היא קטנה (206x219) ותיאורה במקור מערבב בין שני האירועים. לא
"מרככים" כיתוב על בסיס מקור שאני יודע שהוא מבולבל. נרשם ב-`removed` וב-`disputes`.

### פעולות 14–29 · אימות חיות תמונות — נחסם ברמת ה-IP
`curl -sI` על upload.wikimedia.org: הקובץ הראשון החזיר **200 + image/jpeg**,
וכל השאר **429** (rate limit). ניסיון שני עם 5 שניות המתנה בין בקשות — שוב
429 לכולם, כולל לקובץ שהצליח בפעם הראשונה. **מסקנה: זו חסימה ברמת כתובת ה-IP
של ה-VM, לא קובץ מת** — אותה בעיית "כתובת דאטהסנטר" שהתדריך מתאר לגבי Wayback
ויוטיוב. עצרתי כדי לא לשרוף תקציב על בעיה שאינה תלויה בי.

**מה כן מאמת את התמונות:** ה-API של commons.wikimedia.org (נקודת קצה אחרת,
שלא נחסמה) החזיר לכל קובץ `mime=image/jpeg`, רוחב, גובה וכתובת קנונית. קובץ
שאינו קיים היה חוזר `missing`, ואף אחד לא חזר כך. זו ראיה חזקה לקיום, אבל
**היא אינה 200 מ-`curl -sI`** — ולכן נרשמת ב-`unresolved` כפי שהיא ולא
מוצגת כאילו עברה את מבחן כלל 11 במלואו.

### וידאו — הכרעה לשלוח אפס סרטונים
שני הסרטונים שבשורה הקיימת **קיימים ורלוונטיים** (בדקתי דרך oembed):
"Could you patent the sun?" (Global Citizen) ו-"Could You Patent the Sun? The
Polio Vaccine Salk Never Owned" (Patent Doctrine). אבל כלל 16 וכלל 62 דורשים
**תמליל שנמשך ונקרא** לכל סרטון, ויוטיוב חוסם את ה-VM. שרשרת הנפילה מובילה
למחשב של תומר, ואין לי תקציב פעולות לפתוח צינור Whisper בריצה הזאת.

**ההכרעה:** לא לשלוח סרטון בלי תמליל. כלל 60 מתיר זאת במפורש — "אין סרטון?
תמונות מספיקות", ובתנאי שיש 5 תמונות ומעלה; יש שמונה. שני הסרטונים נרשמים
ב-`missing`, ב-`tried` וב-`spinoff_leads` עם המזהים, כדי שהעובד הבא לא יגלה
אותם מחדש ויידע בדיוק מה נשאר לעשות.

### אימות סופי
17 ציטוטים מועמדים נבדקו מילה-במילה מול הטקסט המפושט של הדפים שנמשכו בפועל.
**17/17 נמצאו.** אפס ציטוטים מומצאים, ואפס ציטוטים שנלקחו מהדף הישן בלי בדיקה.

### בנייה ואימות מכני
בניתי את ה-JSON ב-/tmp/work-a8f5/deed.json וכתבתי לצדו `validate.py` שבודק את
שלוש הבדיקות שנופלות בשקט: אורכי כיתוב (כלל 147), היעדר עברית בשדות `_en`
(כלל 20), ורישיון מהרשימה הסגורה (כלל 137) — ובנוסף `sensitive_claims` קיים,
`archived_url` קיים וריק בכל ציטוט (כלל 136), ו-≥5 דומיינים.

שלוש תקלות שהבדיקה תפסה ושלא הייתי מוצא בעין:
1. שתי תמונות (בן-גוריון 1959, CDC 1988) נשארו עם `group=null`. במקום להשאיר
   אותן חריגות נתתי להן קבוצה משלהן — "השנים שאחרי". עכשיו ארבע קבוצות של
   שתיים, ושורת ה-`checked` של כללים 45–46 עודכנה בהתאם.
2. שדות טקסט שנקראים בדף (`aftermath`, `recognition`) נשאו הדגשות Markdown
   `**`. ב-JSON זה נרנדר כתווים ממש. השוואה לריצה גמורה קיימת
   (ff4313ea) הראתה אפס `**` בכל הקובץ. הוספתי `unmark()` שמנקה רקורסיבית.
3. `content_delta` הכיל "מיליון וחצי עד חצי מיליון" — ניסוח משובש של נתון
   שה-WHO מנסח "יותר מחצי מיליון". תוקן.

בנוסף יישרתי את `audit` מול הריצה הגמורה: נוספו `domains_covered`, `missing`,
`tried` ונתיבי שני היומנים, כך שהבדיקה תמצא אותם במקום שהיא מחפשת בו.

**נחיתה:** `mv` יחיד מ-/tmp/work-a8f5 ל-/tmp/enrich-out — הפעולה האחרונה,
כדי ש-`apply_standard_pass.py` לא יבלע קובץ חלקי.

**סיכום תקציב:** ~31 פעולות רשת מתוך 40. status=complete.
