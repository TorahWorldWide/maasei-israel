# דף משימות — מעשי ישראל: הכנה ליציאה

עודכן: 2026-08-07 | מטרה: להוציא את האתר כמה שיותר מהר

---

## נקודת המשך (נכתב בסוף סשן 7.8, מאומת מול גיט + מסד הנתונים + האתר החי)

**הכל שמור ודחוף.** עץ העבודה נקי, `main` מסונכרן, Vercel פרס.

**מה נסגר בסשן הזה:**
- `c25a992` ממשק דו־לשוני he/en + דילוג על סרטוני יוטיוב חסומים בבמה (זו הייתה "העבודה הלא־שמורה" — היא **נשמרה ופורסמה**, ההחלטה הזו סגורה)
- `9bc9e5d` + `3bac3dd` **שתי התוספות שביקשת לדף מעשה**: חצי הבא/קודם בין סרטונים עם מונה מיקום, וקולאז' תמונות מתחת לנגן (עד 10, האריח הראשון גדול, לחיצה פותחת תמונה מלאה, תמונות שבורות מוסתרות). חי באתר.
- `c582a2f` מסמכי בסיס: `audit-tags.md`, `proposed-tags.json`, `audit-sources.md`, `merge-proposals/icq.md`, `dedup.ts`
- **מיזוג ICQ בוצע בפועל** באישורך: 207 ← 206 מעשים, הכותרת הממוזגת חיה באתר

**מה נעצר באמצע (שלוש נפילות מכסה) — ממשיכים מכאן:**
1. **9 המיזוגים שאישרת נותרו לא־מבוצעים.** אושרו לביצוע לפי תבנית ICQ, אך לא רצו. מאומת שעדיין כפולים: יד שרה, עזר מציון, איחוד הצלה, זכרון מנחם, זק"א. עוד לבדוק ברמת התיאור: PillCam, Tevel, IsraAID אפגניסטן, IsraAID איטליה.
2. **פער הנתונים של הקולאז'** — הרכיב בנוי, המידע לא. מתוך 206 מעשים: 81 בלי תמונה כלל, 90 עם תמונה אחת, רק 35 עם 2+. ביקשת ~5 ועד 10 לכל מעשה. צריך צינור איסוף תמונות (מקור + שנת צילום לכל תמונה) — זו העבודה הגדולה הבאה, לא עוד קוד UI.
3. **`src/lib/dedup.ts` מהודר אך לא מחובר** למסך האישור, ועמודת `entries.dedup` עוד לא קיימת במסד. זו משימה 1.

**תוכניות העבודה כתובות ומוכנות לביצוע:** [`docs/plans/`](plans/README.md) — קובץ לכל תוכנית, הסשן טוען רק את מה שהוא מריץ.
סדר שנקבע: **1 שנים → 4 מיזוגים → 2 מקורות → 3 תמונות.**

**בדיקת האמינות (7.8.2026) — הממצא החשוב ביותר עד כה.**
נדגמו חמישה מעשים אקראיים ונבדקו מול מקורות עצמאיים. **ארבעה מתוך חמישה יצאו פגומים**: שנה שגויה (רזאג'ילין 2005 ← 1990), אגדה שהיסטוריונים דוחים (חיים סלומון "מימן את הקונגרס"), ומספרים שהתיישנו (Innovation: Africa 10 מדינות ← 6). חמשת המעשים **תוקנו והמקורות הוחלפו** בפטנט, EMA, הטכניון, Immigrant Entrepreneurship, אתר UBQ ו-Jewish Virtual Library — אך לפי הוראתך הם **לא סומנו כמתוקנים** ונשארים בתור הבדיקה ככל השאר: תיקון אינו אישור, וייתכן שגם אנחנו פספסנו.
נבנה סורק יבש `scripts/audit_claims.py` (אפס טוקנים) שרץ על כל 206: **124 ויקיפדיה בלבד · 65 מספרים שעלולים להתיישן · 21 חשד לשנת פרסום במקום שנת מעשה · 16 חשד לאגדה · 7 סתירות מספר פנימיות · 5 בלי ציטוט · 48 נקיים בסריקה היבשה.**
→ **[תוכנית 5 — מעבר דיוק](plans/plan-5-accuracy.md)**, רצה יחד עם תוכנית 2. זו עדיפות גבוהה מכל השאר: האתר לא יוצא עם ארבעה מתוך חמישה פגומים.

**תוכנית 1 בוצעה (7.8.2026).** פילטר המאות רב־הבחירה חי: שש תקופות עם מונים (1100–1200 · 1, 1500–1600 · 1, 1700–1800 · 1, 1800–1900 · 13, 1900–2000 · 101, 2000–2100 · 89 = 206), איחוד בין תקופות לא־רצופות, `?era=` משותף בקישור (נקרא בשרת כדי שלא יהבהב), ותג תקופה דיסקרטי על כל כרטיס ובדף המעשה. התיוג **נגזר מהשנה בזמן ריצה** — אפס מיגרציות. אימות: `node --env-file=.env.local scripts/verify-eras.mjs` מול המסד, ובדיקת דפדפן (מונים = מסד, איחוד ולא חיתוך, הפילטר שורד רענון).
**הבאה בתור: תוכנית 4 — תשעת המיזוגים.**

---

## מצב קיים (כדי לא לבנות פעמיים)
- `/submit` **כבר חי**: טופס הוספת מעשה — כותרת, הסבר, קטגוריה, שנה, מדיה (תמונה/יוטיוב/העלאה עד 50MB), מקור חובה (אחד) → נכנס לתור המתנה
- `/admin` **כבר חי**: מסך אישור/דחייה של הגשות
- דף מעשה **כבר חי** עם קרוסלת סרטונים + קולאז' תמונות (חסר נתונים, ראה למעלה)
- ממשק דו־לשוני he/en **כבר חי**

## משימה 1 — מנגנון הוספת מעשה (להשלים את החסר)
- [ ] **בדיקת דדופ באישור**: כשתומר מאשר הגשה, הקוד בודק אם המעשה כבר קיים באתר (מזהה סרטון / כותרת דומה / מילות מפתח) → אם קיים, מראה איפה ושואל מה לעשות
- [ ] **אזהרת מדיה חסרה**: אין תמונות/סרטון בהגשה → "בטוח שאתה רוצה לפרסם בלי?"
- [ ] כמה מקורות להגשה (כרגע רק אחד) — יבוצע יחד עם משימה 4 (סכמת מקורות חדשה)
- [ ] כמה תמונות להגשה

## משימה 2 — תפאורה חדשה
- [ ] כיוון: מגניב + ממלכתי + מתאים לכל הגילאים + קל לשימוש
- [ ] להכין 2–3 כיווני עיצוב להשוואה (השראה מאתרי־על, לא "מכני/זול")
- שמורים מהעבר: navy+gold, במה גדולה במרכז, בלי נקודות ניווט

## משימה 3 — תיוגים
- [ ] דוח מצב קיים על כל התיוגים ← **רץ עכשיו**
- [ ] תיוג מדינות ספציפיות (אמריקה, ...)
- [x] תיוג מאות שנים: התיוג נקרא "1900–2000" וכו' — פילטר לפי תקופה (מחושב אוטומטית משדה השנה) — **בוצע (תוכנית 1)**
- [ ] תיוג/פילטר יש־סרטון / אין־סרטון

## משימה 4 — מקורות (הלב של האתר)
- [x] סריקה: כמה פריטים נשענים על ויקיפדיה בלבד — **124 מתוך 206** (`scripts/audit_claims.py`)
- [ ] צינור מקורות: ויקיפדיה → המקורות שהיא עצמה מצטטת → קריאת כל מקור → דירוג מהרלוונטי ביותר → שמירת ציטוט + עמוד/מיקום
- [ ] סכמה חדשה: הרבה מקורות לכל מעשה (10, 20, כמה שיש), ממוינים לפי רלוונטיות
- [ ] כלל ברזל: ויקיפדיה = מקור משני בלבד, לעולם לא יחיד

## משימה 5 — מיזוג כפילויות + איכות תוכן (פסיקות תומר 2026-08-07)
מיזוג = AI קורא את שני הדפים, מאחד את כל המידע והמדיה לדף אחד, ורק אז מוחק את השני (באישור תומר).
- [x] ICQ — **מוזג ובוצע** 7.8.2026 (תבנית לכל השאר: `docs/merge-proposals/icq.md`)
- [ ] PillCam — מיזוג (סרטונים שונים — כולם נשארים בדף הממוזג)
- [ ] יד שרה / עזר מציון / איחוד הצלה / זכרון מנחם / Tevel / IsraAID אפגניסטן / IsraAID איטליה — מיזוג
- [ ] זק"א — למזג את שני דפי־הארגון; דף המקרה הספציפי נשאר נפרד; לאמת שנת ייסוד (1989 מול 1995 — סתירה)
- [ ] אימות שנים בכל האתר עם מקורות (כולל איחוד הצלה 2006)
- [ ] תיקון עברית שבורה ("פאות מתרוממות שיער") + סריקת ניסוח בכל 207 הדפים
- [ ] מקסיקו 2017: כותרת "50 חיילים" סותרת את הסרטון של הדף (70+ חיילים ועוד 25 מהנדסים) — לאמת מספר סופי בתמליל + מקורות כתובים ולתקן
- [ ] סריקה רוחבית: לכל דף עם סרטון — להשוות תמליל מול כותרת/תיאור ולתפוס סתירות עובדתיות נוספות
- [ ] תמיכה בעד 5 סרטונים לדף (סכמה + UI) + בחירת סרטון־על לתיאטרון לפי משיכת תמלולים
- [ ] שדה מקור־מדיה: מאיפה כל תמונה/סרטון + שנת צילום (סכמה + UI + פרוטוקול)
- [x] פרוטוקול החברה עודכן עם כללי האיכות החדשים (docs/company-protocol.md)

## משימה 6 — מעבר דיוק על כל 206 (חדש, 7.8.2026) — [תוכנית 5](plans/plan-5-accuracy.md)
אותו שורש כמו משימה 4: לא עשינו מספיק עבודה על המקורות. **ויקיפדיה אסורה כמקור, מותרת רק כמפת דרכים למקור האמיתי.**
- [x] סורק יבש שמנתב את התור לפי סיכון — `scripts/audit_claims.py`, אפס טוקנים
- [ ] עמודת `audit` במסד: `{state, at, by, checked[], unresolved[]}` — `unaudited`/`audited` בלבד, בלי מצב "תוקן"
- [ ] להריץ את המעבר יחד עם תוכנית 2 — קטיף אחד משרת גם מקורות וגם דיוק
- [ ] אימות ציטוט מכני: כל ציטוט נמצא מילולית בדף החי, אחרת לא מתפרסם
- [ ] רענון מספרים: לכל מספר תאריך שליפה; דיווח עצמי מסומן ככזה
- [ ] בדיקה עיוורת חוזרת על 10% — אחוז אי־ההסכמה מדווח כהערכת שגיאה
- [ ] `docs/defects.md` — מרשם מחלקות הכשל; כל מחלקה הופכת לבדיקה ב-`gate.py`
- [ ] עדכון פרוטוקול: מודל חזק לשיפוט (שנה/טענה/מספר), מודל זול לאיסוף
- [ ] 5 המעשים שתוקנו ב-7.8 עוברים את המעבר המלא ככל האחרים — **בלי סימון "תוקן"**, כי גם המתקן יכול היה לפספס

## החלטות פתוחות של תומר
1. ~~לפרסם את העבודה הלא־שמורה?~~ **סגור** — פורסם ב-`c25a992`
2. סדר העדיפויות בין המשימות ליציאה — **עדיין פתוח, זו השאלה הראשונה לסשן הבא**
