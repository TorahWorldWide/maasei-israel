# יומן עבודה — 55c7771e-3d56-4e79-a08b-70f102c2a5fe

**המעש:** מנחם בגין / הסכם השלום עם מצרים / החזרת סיני
**התחלה:** 2026-08-11
**מצב פתיחה:** בנייה מחדש מאפס (כלל 49). הטקסט הקיים = רמז בלבד.

## 0. קריאת התדריך

קראתי את התדריך במלואו (793 שורות, שני עמודים).

**מה בלט מייד:**
- השורה הקיימת סותרת את עצמה: `year: 1978` אבל התיאור אומר "ב-1979 חתם".
  קמפ דייוויד = ספטמבר 1978, הסכם השלום = מרץ 1979. הכותרת מדברת על
  *חתימת הסכם השלום* ועל *החזרת סיני* — ולכן השנה הנכונה היא 1979.
  צריך אימות חיצוני לשני התאריכים.
- המקור הראשי הקיים הוא ויקיפדיה — נכשל בכלל 1. חייב להתחלף.
- ציטוט אחד בלבד, מוויקיפדיה — נכשל בכללים 2, 3, 6, 32.
- אין `image_provenance`, אין `location`, אין `people`, אין תגים,
  אין 64–67. כלומר: בנייה מלאה, לא תיקון.

**הכרעת קטגוריה (מילון הפרשנות, שדות 64–67):**
התדריך אומר במפורש — "כל מעש שעושיו מתו מזמן ומקורותיו ארכיוניים →
**היסטורי**, ולו קודמת הקטגוריה הזו." בגין מת ב-1992, סאדאת נרצח ב-1981,
המקורות הם ארכיוני קרטר, ארכיון המדינה ומשרד החוץ. ⇒ עמודת **היסטורי**:
- `origin_story` = ההזדמנות/הסכנה שהולידה את המעשה, לפי מסמכים בני-הזמן
- `aftermath` = מה השתנה — **וגם מה שהיסטוריונים אומרים שלא השתנה**
- `recognition` = **המחיר שהמעשה גבה** (אצל היסטורי המחיר הוא התשובה)
- `honors` = רחוב, מוזיאון, לוח זיכרון, בול

**בעיה ידועה מראש — תג סוג המעשה (כלל 38):**
הרשימה הסגורה היא: המצאה מדעית · רפואה והצלת חיים · חסד וצדקה ·
חילוץ ואסון טבע · חינוך · זכויות אדם. **אין בה תג לדיפלומטיה או לשלום.**
לא אמציא תג (כלל 123 מפיל תג מחוץ לרשימה). אבחר את הקרוב ביותר
ואירשום את הפער ב-`unresolved` כדי שתומר יכריע.

**תוכנית מקורות (5+ דומיינים עצמאיים, לא ויקיפדיה):**
1. `nobelprize.org` — פרס נובל לשלום 1978, בגין וסאדאת
2. `history.state.gov` — Office of the Historian, קמפ דייוויד
3. `jimmycarterlibrary.gov` / `catalog.archives.gov` — מסמכים בני-הזמן
4. `mfa.gov.il` — נוסח ההסכם ונאום בגין, מקור ישראלי רשמי
5. `mfo.org` — הכוח הרב-לאומי בסיני, השלמת הנסיגה
6. גיבוי: `knesset.gov.il`, `avalon.law.yale.edu`, `begincenter.org.il`

**תקציב רשת:** 40 פעולות. אספור כל אחת בסעיף הבא.

## 1. מקורות — מה נמשך, מה החזיק, מה נפל

התוכנית מסעיף 0 החזיקה בערך בחציה. הפירוט, לפי סדר הניסיון:

**נפלו מייד (דומיין חוסם ⇒ נטישה מיידית, לפי הוראת התדריך):**
- `nobelprize.org` — HTTP 403. זה היה המקור הראשון בתוכנית. לא ניסיתי שוב.
  עובדת פרס נובל לשלום 1978 הושגה בסוף מ-`jewishvirtuallibrary.org`.
- `britannica.com` — HTTP 403. נטוש.
- `history.com` — HTTP 404 על הדף הצפוי. נטוש.

**נמשכו אך לא נוצלו (עמודי ניווט בלי טקסט רציף):**
- `jimmycarterlibrary.gov/research/camp_david_accords` — עמוד ניווט.
  זו אכזבה אמיתית: זה היה אמור להיות מקור המסמכים בני-הזמן.
- `jewishvirtuallibrary.org/anwar-sadat` — הוחזר ניווט בלבד. **תוצאה ישירה:**
  רצח סאדאת (1981) והשעיית מצרים מהליגה הערבית **לא נטענו בדף**. שתיהן
  רלוונטיות מאוד ל-`aftermath` ולמחיר — ולכן הן ב-`unresolved`, לא בטקסט.
  כלל 51: אין פריט בלי הוכחה. עדיף חור מתועד מאשר עובדה מהזיכרון.
- `begincenter.org.il/en/about-the-center/` — אישר שהמוסד קיים, אבל אין בו
  משפט מתועד על שנת הקמה או מעמד. לכן `honors` נשען על מה שכן אומת.

**החזיקו — שבעה דומיינים, 18 ציטוטים:**
| דומיין | ציטוטים | מה נתן |
|---|---|---|
| `avalon.law.yale.edu` | 2 | נוסח החוזה עצמו, סעיף I — הנסיגה מסיני |
| `db.begincenter.org.il` | 3 | קולו של בגין, בעברית |
| `history.state.gov` | 2 | Office of the Historian — המסלול הפלסטיני |
| `www.csmonitor.com` | 3 | עיתונות בת-הזמן — ימית והשפעתה |
| `m.knesset.gov.il` | 3 | נאום בגין בכנסת, מקור ראשוני רשמי |
| `jewishvirtuallibrary.org` | 3 | נובל, לוח זמנים |
| `www.inss.org.il` | 2 | הערכה ישראלית מאוחרת |

חמישה דומיינים היו המינימום (כלל 2). שבעה זה מה שנשאר אחרי שלושה 403/404.

## 2. הממצא החשוב ביותר בפס הזה — כלי משיכה שיבש ציטוט בעברית

זה הסעיף שאני הכי רוצה שהעובד הבא יקרא.

משכתי את דף מרכז בגין דרך כלי המשיכה. הוא החזיר:
> "אני בא למסור דין-וחשבון לבית..."

הריצותי אימות מילה-במילה מול ה-HTML הגולמי של אותו דף (כלל 4 + כלל 50).
**הציטוט לא נמצא.** הנוסח האמיתי בדף הוא **"באתי למסור"**.

זו לא שגיאת הקלדה. זה כלי שהחזיר עברית שלא כתובה בדף. אילו הייתי סומך עליו,
היה נכנס לדף ציטוט מזויף מיוחס לראש ממשלת ישראל — בדיוק סוג הכשל שכלל 4 קיים
כדי למנוע. שני ציטוטים עבריים נוספים חזרו משובשים בעליל ("צריך להתבונן עתידה")
ונזרקו בלי שימוש.

**מה עשיתי מרגע זה והלאה:** כל ציטוט עברי אומת בשיטה אחת בלבד —
משיכת HTML/PDF גולמי, נרמול (רווחים, סימני כיווניות, וריאנטים של מקף
ומרכאות), והתאמת תת-מחרוזת מדויקת בפייתון. אף ציטוט לא נכנס לפלט על סמך
סיכום של כלי.

הממצא הסותר **הושאר במקומו** ביומן הידע ולא נמחק, לפי כלל 135.

**PDF של הכנסת:** כלי המשיכה סירב לנתח את הקובץ (בינארי) — אבל שמר אותו
מקומית. `pdftotext -enc UTF-8` הוציא את הנאום המלא. RTL בפייתון עושה שני
נזקים: פיסוק מודפס *לפני* המילה ("שלום מלא ,אמיתי") ומילים נשברות
("הניסי ון"). לכן בחרתי רק מקטעים רצופים בלי פיסוק פנימי. השיטה מתועדת
ביומן הידע.

## 3. הכרעות תוכן

**השנה — תוקנה מ-1978 ל-1979.** קמפ דייוויד = ספטמבר 1978, החוזה = 26.3.1979.
הכותרת מדברת על חתימת החוזה ועל החזרת סיני ⇒ 1979. אומת בשני מקורות עצמאיים.

**64–67 בעמודת היסטורי, כפי שנקבע בסעיף 0:**
- `aftermath` נושא גם את **מה שלא השתנה**: משרד החוץ האמריקאי על המסלול
  הפלסטיני שהתברר כבלתי-גשיר, ו-CSM על כך שימית גרמה לרוב הישראלים להישבע
  שלא יעזבו את יהודה ושומרון ואת עזה. זה לא קישוט — זו דרישת כלל 65.
- `recognition` = **המחיר**, ולא כבוד. העוגן הוא מילותיו המאומתות של בגין
  עצמו: "אחד הקרבנות — את הכאב עליו איני מסוגל להביע בשפת אנוש".

**מספר היישובים שפורקו: 14, לא 16.** רק 14 (ימית ושלושה-עשר) אומת בדף חי.
"16" הופיע בסיכום מנוע חיפוש בלבד. לפי כלל 30 נכתב המספר המאומת עם ייחוס,
והפער נרשם ב-`disputes`.

**מיקום:** הבית הלבן, וושינגטון, `precision: exact` — שם נחתם. הערה בשדה
מסבירה שסיני עצמו במצרים; לא המצאתי עיר.

**תג סוג המעשה — הפער שנחזה בסעיף 0 התממש.** ברשימה הסגורה אין דיפלומטיה,
שלום או מדינאות. לא המצאתי תג (כלל 123). נבחר "זכויות אדם" כקרוב ביותר,
עם רישום מפורש ב-`unresolved` שזו החלטת ברירה ולא התאמה, ועם שאלה לתומר
האם להוסיף לרשימה תג "שלום ומדינאות".

**`sensitive_claims: []`** — רשימה ריקה היא תשובה. כל הנפשות הפועלות נפטרו
(בגין 1992, סאדאת 1981, קרטר), ולכן הסטנדרט הכפול לחיים אינו חל.

## 4. תמונות ורישיונות

לא ניחשתי שמות קבצים. השתמשתי ב-API של ויקישיתוף בבת-אחת —
`categorymembers` + `search` + `imageinfo&iiprop=url|mime|size|extmetadata` —
וקיבלתי רישיון, צלם, תאריך ותיאור לכל הקבצים בקריאה אחת. **זו ההמלצה
המרכזית שלי לעובדים הבאים; היא חוסכת עשרות פעולות רשת.** רשומה ב-`tried`.

**נכללו: 5 תמונות**, כולן אומתו חיות ב-`200 + content-type: image/jpeg`,
עם `image_provenance` מלא: `license` מהרשימה הסגורה, קרדיט, מתי צולם,
`caption_he` עד 80 תווים בשורה אחת + `caption_long_he` להסבר הארוך.
שתי קבוצות: "מנחם בגין, ראש ממשלת ישראל" ו"החתימה על מדשאת הבית הלבן, 26.3.1979".

**לא נכללו — 3 תמונות, 429:** `upload.wikimedia.org` החזיר
Too Many Requests לכתובת ה-IP של ה-VM בשלוש ריצות נפרדות: HEAD, HEAD עם
ריווח 25 שניות, ו-GET מוגבל-טווח, ובנוסף ריצת רקע עם המתנות של 150+30 שניות.
זו אותה מגבלת IP של דאטהסנטר שהתדריך מתאר לגבי Wayback. הרישיונות והתאריכים
שלהן כבר אומתו ורשומים ב-`unresolved` — מהמחשב הביתי של תומר הן צפויות לעבור.

**לא נכללו — צילומי פינוי ימית 1982 (ניצן דנציג).** זה החומר החזותי החזק
ביותר למחיר שההסכם גבה, ולכן ההחלטה לא הייתה קלה. הרישיון בוויקישיתוף הוא
תבנית `{{Attribution}}`, שאינה וריאנט CC מוגדר ואינה נופלת נקי לאף ערך
ברשימה הסגורה של כלל 137. לא סיווגתי אותה כ-`cc` — הצהרת רישיון לא מדויקת
היא בדיוק מה שכלל 137 בא למנוע. השאלה מובאת להכרעת תומר.

## 5. תקציב רשת

כ-30 מתוך 40 פעולות נוצלו. הפירוט הגס: 3 בזבוזי 403/404, 2 עמודי ניווט,
כ-14 משיכות תוכן שהניבו ציטוטים, 1 קריאת API מרוכזת לוויקישיתוף,
כ-10 בדיקות חיות של תמונות (מהן 3×3 כשלונות 429).

התקציב **לא** נגמר, ולכן `status: "complete"` ולא `partial`, ו-`missing: []`.
מה שחסר אינו חסר מחוסר תקציב אלא מחוסר מקור שנמשך בפועל — וזה רשום
ב-`unresolved` עם הכתובת הסבירה לאימות עתידי.

## 6. מה לא עשיתי, בכוונה

- **לא הרצתי `archive_source.py`.** כל `citations[].archived_url` ריק.
  18 ציטוטים ממתינים לארכוב המרוכז, לפי הוראת התדריך — ערוץ SSH אחד
  למחשב של תומר, ומתחרים עליו מאבדים סנפשוטים ששולם עליהם.
- **לא נגעתי במסד, לא הרצתי SQL, לא עשיתי git commit.** הכותב היחיד
  הוא `apply_standard_pass.py`.
- **לא המצאתי תג, לא המצאתי עיר, לא תפרתי שני משפטים לציטוט אחד.**

## 7. סיכום הפס

תוקנה שנה שגויה, הוחלף מקור ויקיפדיה יחיד ב-18 ציטוטים מאומתים על פני
7 דומיינים עצמאיים, נבנו מאפס 64–67 בעמודת היסטורי, נוספו 5 תמונות עם
פרובננס ורישיון מלאים, מיקום, אנשים, תגים, `sensitive_claims`,
16 רשומות `content_delta`, 3 תיקונים, 2 מחלוקות ו-7 פריטים פתוחים.

הלקח הראוי להעברה: **כלי משיכה אינו מקור.** ציטוט עברי מאומת רק מול
HTML גולמי, לעולם לא מול סיכום.
