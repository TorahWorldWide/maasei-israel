# יומן עבודה — c1848f94 · סלמן ואקסמן והסטרפטומיצין

**התחלה:** 2026-08-11
**מצב פתיחה:** בנייה מחדש מאפס (כלל 49). השורה הקיימת נקראה כרמז בלבד.

## תוכנית
- קטגוריה: המצאה מדעית + רפואה והצלת חיים → מילון פרשנות "המצאה מדעית".
- הלב הדרמטי המשוער (לאימות מול מקורות, לא להנחה): מי בודד בפועל את הסטרפטומיצין,
  ומה קיבל מי. אם יש פער בין המעשה להכרה — זה הסיפור (כלל 66).
- תקציב רשת: 40 פעולות. אספור כל curl/fetch.

## יומן פעולות

### שלב א׳ — הכנה (0 פעולות רשת)
קראתי את התדריך במלואו לפני שנגעתי ברשת. פתחתי את יומן הידע לפני המקור הראשון
(כלל 133). בניתי `/tmp/striphtml.py` — ממיר HTML לטקסט — כדי שכל בדיקת ציטוט
תיעשה מול הדף שנמשך בפועל ולא מול הזיכרון שלי.

**החלטה מוקדמת:** לא אשמור טיוטות ב-`/tmp/enrich-out/`. שם רץ ה-watcher שבולע כל
`*.json` בתיקייה, וקובץ חלקי שייבלע הוא כתיבה למסד שלא התכוונתי אליה. כל הבנייה
נעשתה ב-`/tmp/`, והקובץ הסופי נחת ב-`os.replace` אטומי.

### שלב ב׳ — המקורות שנמשכו (6 דומיינים)
1. **nobelprize.org** — שני דפים, facts ו-biographical. 200.
2. **sebs.rutgers.edu** — מוזיאון ואקסמן. 200.
3. **pmc.ncbi.nlm.nih.gov** — קרופטון, J R Soc Med 2006. 200.
4. **jameslindlibrary.org** — הרשומה על ניסוי ה-MRC 1948. 200.
5. **who.int** — דף עובדות שחפת. 200.
6. **njdigitalhighway.org** — יחידת הארכיון על תביעת שץ. 200.

ויקיפדיה שימשה כמפת דרכים להערות השוליים שלה בלבד, ואינה מצוטטת באף שורה.

### שלב ג׳ — מה נפל, ולמה נטשתי מיד
- **acs.org** — Incapsula. 212 בתים עם `noindex,nofollow`. ניסיתי נתיב שני
  (molecule-of-the-week) — נחסם גם הוא. נטשתי את הדומיין לפי התדריך.
  **המחיר האמיתי:** השורה הקיימת נשענה על acs.org בשני ציטוטים, ואיתם נפלה הטענה
  "ואקסמן טבע את המונח antibiotic". חיפשתי לה מקור חלופי בחמשת הדומיינים
  האחרים ולא מצאתי. לכן הורדתי אותה מהדף ורשמתי ב-`unresolved` במקום להעביר
  אותה מהשורה הישנה כאילו אומתה. זה בדיוק המקרה שכלל 49 קיים בשבילו.
- **sciencehistory.org** — 404 בשני נתיבים. **asm.org** — 404.
  **journals.asm.org** — 403. **thelancet.com** — 403. **time.com** — 406.
  **pubmed.ncbi.nlm.nih.gov** — reCAPTCHA; עקפתי דרך pmc.ncbi.nlm.nih.gov,
  שהוא אותו מאמר בגישה פתוחה.

### שלב ד׳ — פעולת רשת מבוזבזת, מתועדת
חיפוש בוויקישיתוף על "Albert Schatz streptomycin" החזיר **ליברטים של אופרה**.
יש ליברטיסט מהמאה ה-19 באותו שם, והחיפוש נעל עליו. פעולה אחת מהתקציב הלכה.
עברתי לשאילתות לפי קטגוריה ולא לפי שם.

### שלב ה׳ — ההכרעות המהותיות

**הכותרת הקיימת הופרכה, לא שופצה.** "האנטיביוטיקה הראשונה שריפאה שחפת" נופלת
מול קרופטון, שהריץ בעצמו זרוע של ניסוי ה-MRC: הטיפול לא נסמך עליו לרפא שחפת
בגלל עמידות שהתפתחה כבר ב-1948. "נגד" נכון; "שריפאה" לא. כלל 72.

**השנה הוזזה 1952 → 1943.** המעש הוא הבידוד, לא הטקס. ואקסמן עצמו מתארך את
הסטרפטומיצין ל-1943 ברשימת האנטיביוטיקות שלו.

**הפער בין המעשה להכרה אושר, ולא הונח.** שלושה דומיינים עצמאיים — דף העובדות
של ועדת נובל עצמה, רוטגרס, וקרופטון — כותבים שהבידוד בפועל נעשה בידי אלברט שץ,
סטודנט לדוקטורט. באותו דף עצמו של ועדת נובל: `Prize share: 1/1`. רוטגרס, בעלת
המעבדה, מכנה אותו היום "מגלה-שותף". זה הפך ללב `recognition`.

**מחלוקת נרשמה במקום להיבחר (כלל 30).** נובל: "for his discovery of streptomycin".
רוטגרס: "for his methods that led to the discovery". ההבדל אינו סמנטי — נוסח
אחד מותיר מקום לתלמיד והשני לא. שניהם בדף, בלי הכרעה.

**המשקל הנגדי האנושי ל-`recognition`.** מהדף הביוגרפי: רוב התמלוגים הלכו לבניית
המכון; מהחלק הקטן שיועד לו אישית הקימו הוא ואשתו קרן למיקרוביולוגיה ומלגה
ברוטגרס "for an immigrant student, or the son or daughter of an immigrant" —
בדיוק המעמד שבו הוא עצמו נכנס לרוטגרס ב-1911 על מלגת מדינה. בלי זה `recognition`
היה כתב אישום; איתו הוא סיפור.

**היכן בחרתי בכנות על פני נוחות:**
- `precision: "city"` ולא `exact`. מקום הגילוי ידוע בשם, אבל לא נמצאו קואורדינטות
  במקור שנמשך, ואין להמציא נקודה על מפה.
- הורדתי תמונה של Instituto Butantan — לא הצלחתי לאמת מה היא מציגה.
- הורדתי "יהודי-אמריקאי" מהפתיח. אף אחד משישה הדומיינים אינו קובע זאת במפורש.
  זו טענה שכנראה נכונה, ובכל זאת לא מקורה — ולכן היא ב-`unresolved` ולא בגוף הדף.
- הסכום $380,000 מוצג כטענה של שץ, כי המקור עצמו מסמן אותו כך.
- אפס סרטונים. כלל 60 מתיר זאת עם שמונה תמונות, ושרשרת התמליל של כלל 62 דורשת
  מכונה שאינה בהישג המעבר. רשום ב-`missing` ולא מוסתר.

### שלב ו׳ — התקלה שהעלימה את התקציב
תשע בדיקות חיות התמונות החזירו **429 כולן**. חשדתי בחסימת קצב, ואז הסתכלתי
ב-User-Agent ששלחתי: `"maasei-israel/1.0 (image liveness check)"` — בלי כתובת
קשר. ויקישיתוף דורש UA תיאורי עם דרך ליצור קשר. חזרתי עם UA מלא והשהיה של
שנייה בין בקשות; תשע מתוך תשע החזירו `200 | image/jpeg`. הסבב החוזר בלע את
היתרה והנחית אותי על **40/40 בדיוק**. הלקח: תקלת 429 היא לפעמים באג בבקשה שלי,
לא קיר מהצד השני — ובדיקה של הבקשה לפני ניסיון חוזר הייתה חוסכת תשע פעולות.

### שלב ז׳ — בדיקת הציטוטים
31 בדיקות מילה-במילה מול ה-HTML שנמשך. 30 עברו. אחת נפלה: המשפט הפותח של הדף
הביוגרפי, כי הדף מרנדר את ה-S הראשונה כאות פתיח מוגדלת והטקסט קורא "S elman".
קיצרתי את הציטוט כך שיתחיל אחריה, במקום לתקן את הטקסט שנמשך.

### שלב ח׳ — כתיבה
בניתי `parts.json` (ציטוטים + תמונות), `deed_part1.json` (התוכן), ואז
`deed_part2.json` (הביקורת). המיזוג רץ דרך ולידטור שבודק: כל 49 השדות קיימים,
אין אות עברית באף שדה `_en` (כולל בתוך אובייקטים מקוננים), כל שורת ציטוט נושאת
`quote_en`/`locator_en`/`source_label_en` ו-`published`, כל `archived_url` ריק,
כל רישיון מהרשימה הסגורה, וכל כיתוב בתוך מגבלת התווים.

הוא נפל בפעם הראשונה: שלוש רשומות תמונה נשאו `shot_when_en: "לא ידוע"` — עברית
בשדה אנגלי, כי פונקציית העזר שלי העתיקה את `shot_when` לתאום האנגלי אוטומטית.
תוקן ל-`"unknown"`. זו בדיוק הבדיקה השקטה שכלל 20 נבדק בה על שורת הציטוט
ולא רק על גוף הדף, והיא הייתה עוברת מתחת לרדאר בלי ולידטור.

### מה לא עשיתי, במכוון
לא נגעתי במסד. לא הרצתי SQL. לא הרצתי `archive_source.py` (כלל 136) —
`archived_url` ריק בכל 28 הציטוטים, והארכוב מרוכז ורץ אחרי הכתיבה. לא עשיתי commit.

**סטטוס:** `complete`. 6 דומיינים · 28 ציטוטים · 8 תמונות · 40/40 פעולות רשת.
