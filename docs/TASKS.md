# דף משימות — מעשי ישראל

עודכן: 13.8.2026 · https://maasei-israel.vercel.app

**קרא קודם:**
1. [`DEED-STANDARD.md`](DEED-STANDARD.md) — התבנית. מה זה דף מעשה מוגמר. **זה המסמך העיקרי.**
2. [`LEARNINGS.md`](LEARNINGS.md) — איך להריץ סוכנים בלי שימותו ובלי לשרוף טוקנים.

---

# משימה ראשית — להביא את כל 201 המעשים לתקן

זו **המשימה**. כל השאר בדף הזה קטן ממנה.

**שני מספרים, ולא אחד (הכרעת 12.8, ס13 בתקן).** הבדיקה האוטומטית מודדת ש**שדה מלא**,
לא שמה שכתוב בו נכון — ולכן "50/50" נקרא מהיום **"שדות מלאים"**. המספר שמבטיח משהו
לגולש הוא **"אומת"**: שרד ביקורת אדברסרית (כלל 140). רק הוא רשאי להופיע כסימון באתר.

**המצב היום (13.8, בתום פאס ההשלמה): 12 שדות-מלאים 58/58 · 23 אומתו.**
כל 23 המעשים עברו ביקורת אדברסרית (כלל 140) — זה המספר שמבטיח משהו לגולש.
ה-12 הם מי שגם עומדים בכל 58 הבדיקות המכניות אחרי סבב התיקון.

```bash
python3 scripts/deed_standard.py              # טבלת מצב מלאה
python3 scripts/deed_standard.py --failing 2  # מי נכשל בקריטריון מסוים
python3 scripts/deed_standard.py --fields     # שדה תוכן שהאתר לא מציג (כלל 152)
```

### נקודת המשך — 13.8 · פאס ההשלמה נסגר

הדרייבר (`scripts/verify/drive_pass.sh`) רץ 12:26–18:37 UTC על 23 המעשים והשלים את כל
השרשרת לבדו: השלמה ← החלה ← ארכוב ← ביקורת אדברסרית ← החלה ← פאס תיקון ← החלה.

**התוצאה:** 23/23 אומתו · 12/23 גם 58/58 מכני.

הביקורת האדברסרית בדקה 668 טענות והחזירה **221 ממצאים — 26 חמורים, 93 בינוניים,
102 קלים**. אף מעש לא חזר נקי. הממצאים החמורים חוזרים על דפוס אחד: הדף מציג ציטוט
שסותר את מה שהוא עצמו כותב, בלי מילת יישוב.

**11 המעשים שנשארו מתחת ל-58 — ולמה.** אף אחד מהם אינו נופל על 149 (תיאור עיוור),
כלומר לא נשאר תיקון זול; מה שנשאר דורש מקור נוסף או הכרעת עריכה:

| נכשל | כמה | מה זה דורש |
|---|---|---|
| 148 — מבחן המספרים | 8 | מספר בדף בלי ציטוט תומך או חישוב רשום |
| 147 — כיתוב שורה אחת | 4 | כיתוב ארוך מדי בלי נימוק חריגה |
| 20 — תרגום אנגלי מלא | 4 | שדה תוכן שלא תורגם |
| 67 — כבוד על שמם | 2 | שם העושה חסר במקום שהתקן דורש |
| 32 — תאריך פרסום למקור | 1 | מקור בלי תאריך |
| 150 · 160 | 1 כ״א | פער פתוח חוסם פרסום · תיקון שלא סונכרן לשתי השפות |

**מה פתוח:** לסגור את 148 (הכבד מכולם) דורש חזרה למקורות, לא ריצת סוכן נוספת.
משמרת הלילה `maasei-company-shift` **נשארת כבויה** עד הודעה מתומר.

### נקודת המשך — 13.8 · הפסיקה על הכתיבה

תומר קרא את הדף הקנוני של לאמאר ופסק: **העובדות בסדר גמור, הכתיבה לא.** מכאן שלושה
כללים חדשים, 167–169, וכולם כבר בתקן ובקוד:

- **167 — ויקיפדיה כהשראת כתיבה, לעולם לא כמקור.** הסגנון הנדרש הוא ערך אנציקלופדי
  מעולה: ברור, זורם, מכובד. כללים 1 ו-53 לא זזו — לא עובדה אחת משם.
- **168 — הכותרות שייכות לדף.** `sections[]` נכנס למסד ולתצוגה; שדות התוכן
  (`origin_story`/`act`/`ripple`/`aftermath`/`recognition`) **נשארים דרישה מלאה**
  ומזינים את דף הבית ואת הבדיקות. מה שבוטל: התווית הקבועה מעל כל פסקה.
- **169 — אינפובוקס בדף ממוקד־אדם.** `infobox` במסד, `DeedInfobox.tsx` באתר,
  לצד התמונה הראשית ב-`xl` ומתחתיה במסך צר. **שורה בלי `source` אינה מוצגת.**

`canonical_type` · `sections` · `infobox` נוספו למסד (202 שורות, ללא שינוי).
התדריך המעודכן: [`WRITER-BRIEF.md`](../scripts/verify/WRITER-BRIEF.md), פרק "הצורה".
הרקע המלא: לקח 31 ב-[`LEARNINGS.md`](LEARNINGS.md).

**מה פתוח:** לאמאר נכתבת מחדש בגישה החדשה ועולה לאישור תומר. תשעת הדפים הקנוניים
הנותרים (כלל 166) ממתינים לאישור הזה — הם ייכתבו לפי הצורה שתאושר, לא לפני.

### ערכת הסגנון לכותב — מוכנה, לא מופעלת (13.8)

הכתיבה כבר נפלה פעם אחת על סגנון, ולכן הערכה הוכנה מראש ונכנסה לתדריך. **היא אינה
מופעלת רטרואקטיבית על לאמאר v2** — תומר טרם פסק עליה, ודף שנכתב מחדש אחרי כל פסיקה
הוא תפר על תפר (163). **מתי היא נכנסת לפעולה:** אם תומר יאמר "לא זה" על v2, לאמאר
נכתבת איתה מחדש; בכל מקרה היא חלה על תשעת הדפים הקנוניים הבאים.

1. **שלוש דוגמאות פרוזה עברית מעולה** (הרחבת 167) ב-`WRITER-BRIEF.md`, מתוך הרשימה
   הרשמית של ערכים מומלצים בוויקיפדיה העברית — 749 ערכים, מהם 170 ביוגרפיות של
   אנשים, ומהן שלוש: **אריאדנה סקריאבינה · די. בי. קופר · טום קרין**. לצד כל אחת
   "מה לגנוב", ואצל קופר גם "מה לא לגנוב". סגנון בלבד; אף עובדה משם אינה נכנסת לדף.
2. **אחד־עשר איסורי טעם AI** (כלל 171) — רשימה סגורה בתדריך הכותב, שנגזרה מסקיל
   `humanizer` ומפרומפטי 去AI味.
3. **מעבר עורך סגנון** (כלל 170) — שלב בצנרת בין הכתיבה לבדיקה, תדריך
   [`STYLE-EDITOR-BRIEF.md`](../scripts/verify/STYLE-EDITOR-BRIEF.md): שתי קריאות
   (נער בן 15 משועמם, ואחריו עורך קשוח), הערות סגנון בלבד, שני סבבים לכל היותר.

### נקודת המשך — 12.8

התקן עודכן ל-158 כללים והבדיקות המכניות נכתבו. **מה שנשאר לפני האצווה הבאה:**

1. **68 `caption_long_en` חסרים** — הכיתוב הארוך מוצג מהיום בלייטבוקס, ולכן נכנס
   לכלל 20. בעברית 75, באנגלית 7. עד שיושלמו, הגילוי הנאות אינו נראה באנגלית.
2. **הדף הקנוני (כלל 155)** — מעש אחד נכתב עד ש"זה", ונכנס לתדריך. **חוסם את האצווה:**
   בלעדיו כל עובד ממציא סגנון משלו. **מ-13.8 הוא נכתב בשיטה החדשה** (הפרדת מחקר
   מכתיבה, כללים 163–165): חוקרים ממלאים את יומן הידע, ורק אחר כך כותב מקצועי
   מלחין ממנו את הדף כולו בשתי השפות. שני התדריכים:
   [`RESEARCH-BRIEF.md`](../scripts/verify/RESEARCH-BRIEF.md) ·
   [`WRITER-BRIEF.md`](../scripts/verify/WRITER-BRIEF.md).
   **המשימה הראשונה בשיטה: הדי לאמאר** — היומן שלה נבנה מחדש (כולל תשובות
   לשאלות-הקורא שהביקורת החזירה), ומהיומן נכתב הדף הקנוני לאישור תומר.
   **כלל 163 נשאר 📋 בינתיים:** החצי המכני שלו (`audit.authorship`) נכנס לבדיקה
   רק כשהצינור יכתוב אותו — כלל שאין מאחוריו כותב נכשל בשקט על כל האתר ומייצר
   אדום קבוע (לקח 26, K7).
3. **מיגרציה לשדות החדשים** — `numbers[]`, `image_seen`, `the_moment`,
   `discrepancies[]`, `adversarial_review`, ו-`honors[]` כאובייקטים ולא מחרוזות.
4. **תיקון 5 הממצאים החמורים** בדפים שכבר חיים (טורקיה ×2, מורן, מונטיפיורי, פריאר,
   ותמונת NIST בשכטמן).

### הכלל שמונע שריפת טוקנים

**מעבר אחד לכל מעש, לא שישה מעברים.** קטיפת מקורות אחת טובה סוגרת בבת אחת את
קריטריונים 1, 2, 3, 4, 6, 8, 11, 12, 27, 32, 37 — ובדרך מתקנת גם את 18, 21, 22.
לפתוח את אותם דפים פעמיים זה גם בזבוז וגם סיכון לשתי מסקנות סותרות.

**לכל מעש תקציב קשיח.** נגמר התקציב → כותבים מה שיש, רושמים מה חסר, עוברים הלאה.
מעש אחד לא שורף את המכסה. (קריטריונים 33–36 בתקן.)

### שלושה מצבים בסוף כל מעש

| מצב | מה עושים |
|---|---|
| ✅ `complete` | עומד בתקן, מתפרסם |
| ⚠️ `partial` | חסר דבר או שניים — נרשם במפורש `missing: ["images"]`, מתפרסם, חוזרים |
| ⛔ `exhausted` | חיפשנו באמת ואין. **עומד בתקן** — התקן דורש מיצוי, לא כמות |

`partial` ו-`exhausted` נכנסים לקבוצה נפרדת שאפשר לסרוק במבט: מה חסר, ומה כבר נוסה.

### שלושה שלבים, בסדר הזה

**שלב 0 — מיגרציה אחת (בלי סוכנים, בלי טוקנים).**
שבעה קריטריונים הם 0/201 רק כי העמודות לא קיימות. זה תנאי לכל השאר:
`location` · `people` · `deed_type` · `actor_type` · `beneficiary` · `audit` ·
`citations[].published` · `dedup`.
כתיבה למסד **לא דרך PostgREST** — המפתח anon ו-RLS מחזיר 200 עם גוף ריק, כישלון
שנראה כמו הצלחה. הנתיב: Supabase Management API עם SQL ישיר, ואז קריאה חוזרת לאימות.

**שלב 1 — המעבר עצמו, 201 מעשים.**
תור לפי סיכון מ-`scripts/audit_claims.py`. התבנית שכבר עובדת:
`scripts/verify/launch_merge.sh` + `ENRICH-BRIEF.md`, סוכנים מנותקים,
ואז `verify_best.py` לפני כל כתיבה למסד.
**מודל העובדים: Opus 5.0 — לא Sonnet** (החלטת תומר 9.8: "סונט מטומטם מידי").
שליטה בעלות דרך תקציב קשיח לכל מעש, לא דרך מודל זול.

**שלב 2 — ביקורת-פתע אדברסרית על 10% (כלל 140).**
מדגם אקראי מכל אצווה נבדק מחדש בידי **עובד טרי בלי שום הקשר**, שתפקידו להפריך ולא
לאשר — בודק שקרא את האצווה נסחף איתה. אחוז אי-ההסכמה מדווח כהערכת שגיאה של האתר. אין מצב "תוקן" — מעש שתוקן חוזר לתור
ככל השאר, כי גם המתקן יכול היה לפספס.

### מה כבר סגור מהמעבר הזה

- **תשעת המיזוגים בוצעו** (9.8): 211 ← 202 שורות. 159/159 ציטוטים אומתו מילה-במילה
  מול הדף החי במאמת עצמאי, לא בדיווח עצמי של הסוכן.
- **5 המעשים הפגומים תוקנו** במסד וחיים באתר — ולא סומנו כמתוקנים, בכוונה.
- **פילטר תקופות חי** — נגזר מהשנה בזמן ריצה, אפס מיגרציות.
- **ממשק דו-לשוני מלא** (9.8): הכפתור מחליף את **כל** האתר, כולל `alt`/`title`.
  האתר נפתח בעברית בישראל ובאנגלית בשאר העולם, לפי `x-vercel-ip-country`, בצד השרת
  ובלי הבהוב. נמדד: 1358 מחרוזות עבריות ← 0 אחרי לחיצה.
- **ארבע ההכרעות של תומר יושמו** (11.8): התג `לעם ישראל` החליף את `לישראלים` (העם,
  לא המדינה — כולל את כל מי שחי כאן היום ואת בני העם בגלות בכל הדורות); **יומן ידע**
  נכנס כחובה, כללים 133–135 — קודם היומן, אחר כך הדף, ורק ממנו; דרישת התמליל
  **נשארה** והמסלול עובר במחשב של תומר (`scripts/transcribe_on_pc.py`, כלל 62);
  ושתי שאלות המחקר נסגרו — עזר מציון הוא הפרש של עשור ולא סתירה, ול-UBQ **כן** יש
  אימות חיצוני אך הטווח בדף נשאר.
- **אחת-עשרה ההצעות אושרו ונכנסו** (11.8): כללים 136–145 — ארכוב מקור ברגע הציטוט ·
  רישיון שימוש לכל תמונה · "נכון ל-" על כל מספר מצטבר · רף כפול לטענה על אדם חי ·
  ביקורת-פתע אדברסרית על 10% · תוכן מהרשת הוא נתונים ולא הוראות · תמליל נשמר ביומן
  הידע · נגישות (ת"י 5568) · מטא-תגי שיתוף · אימות דיפלוי אחרי כל דחיפה. בנוסף
  **כלל 94 רוכך** — אין יותר תמריץ לשרוף מכסה, רק עבודה שכבר ממתינה בתור.
  **התקן עומד על 145 כללים**, 49 מהם נבדקים אוטומטית. (כלל 146 נוסף 11.8 —
  תמונות שמקפיצות את העין, עד 2 סריקות טקסט לדף.)
- **שלושת הכללים החדשים קיבלו כותב** (לילה 11–12.8): כלל 136 הראה 0/201 כי אף
  סקריפט לא מילא `citations[].archived_url` מעולם. `scripts/archive_deed.py` ממלא
  אותו למעש שלם בריצה טורית אחת דרך המחשב של תומר, ומה שלא נשמר נרשם
  `archive_failed:<url>` ב-`unresolved`. מסלול התאימות (`COMPLIANCE-BRIEF.md` +
  `launch_compliance.sh` + `compliance_context.py`) סוגר 137/147/20/139 על מעשים
  שהמחקר שלהם כבר אומת, בלי לתת לעובד לגעת בטקסט. ראה לקח 26.
- **הלילה רץ בלי סוכן** (11–12.8): קרון מערכת מריץ כל 20 דקות את
  `scripts/night_apply.sh` — כותב, מארכב כל יעד שעדיין נכשל ב-136, ומנקד. דף
  הסקירה (`scripts/build_review_page.py`) מתפרסם כל חצי שעה ל-GitHub Pages
  בכתובת https://torahworldwide.github.io/maasei-review/ — תומר עובר שם מעש-מעש
  ומסמן "מרוצה" או "צריך תיקון".

---

# משימות אתר — קטנות, לא קשורות למעבר

## 1. חלק א׳ / חלק ב׳ יוצאים מהמסך באנגלית
הפסים הצדדיים **בדף הבית** (התיאטרון), שקוראים `act`/`ripple`. נמדד ב-1280px:
בעברית `left 23 / 1174` (בתוך המסך), באנגלית `left -18` וגם חורג ל-1282. תיקון LTR
של המיקום. **כלל 168 לא ביטל את זה** — הוא שחרר את התצוגה בדף המעש, לא בדף הבית,
ושני השדות עדיין נכתבים ועדיין מוצגים שם.

## 2. כפתור "מצאתי טעות"
המדווח מסביר את עצמו ומצרף מקורות משלו. נכנס לתור ידני, לא משנה כלום אוטומטית.

## 3. לחבר את `src/lib/dedup.ts` למסך האישור
מהודר אך לא מחובר. **שורש הכפילויות:** הבדיקה השוותה מזהה סרטון בלבד — שורה בלי
סרטון אף פעם לא התנגשה ותמיד נכנסה. צריך גם השוואת כותרת/מילות מפתח.

## 4. השלמות מסך ההגשה
אזהרה כשאין מדיה · כמה מקורות ולא אחד · כמה תמונות.

## 5. תפאורה חדשה
מגניב + ממלכתי + לכל הגילאים. 2–3 כיוונים להשוואה. שמור מהעבר: navy+gold,
במה גדולה במרכז, בלי נקודות ניווט.

## 6. מפה עולמית — אחרונה
תלויה בקריטריון 27 (מיקום עם `precision`). בלי מיקומים אין מה להציג, ולכן היא
אחרי המעבר ולא לפניו.

## 7. תשתית לבדוק בפתיחה
- `maasei-company-shift` **מושהה** מ-7.8 → `hermes cron resume maasei-company-shift`
- `maasei-linkrot-watchdog` נפל בטיימאאוט 120 שניות — צריך יותר זמן או ריצה מקבילית
- **אימות דיפלוי אחרי כל דחיפה** (כלל 145) — ב-11.8 שני בילדים נפלו בשקט על תקלת
  גופנים ב-Vercel. צריך בדיקת `READY` בסקריפט הדחיפה, לא בעין

## 8. נגישות — כלל 142
`alt` לכל תמונה (ממוחזר מ-`caption_he` ומהמקבילה האנגלית), ניגודיות צבע תקינה,
וניווט מקלדת מלא. היעד: **תקן ישראלי 5568** — לאתר ציבורי ישראלי זו חובה חוקית,
ולא רק איכות. זול עכשיו עם 201 דפים, יקר אחרי אלף.

## 9. מטא-תגי שיתוף — כלל 143
`og:title` מהכותרת · `og:description` מהתקציר (130) · `og:image` מהתמונה הראשית.
שיתוף בוואטסאפ הופך מקישור עירום לטריילר. שתי שורות בכל דף מעש.

## 10. כיתובי טריילר ותרגום גלריה — 10 הדפים הנותרים
דף הדי לאמאר עבר ב-11.8: כיתוב = שורה אחת (כלל 147), ואנגלית מלאה לגלריה
ולציטוטים (כלל 20 המורחב). שאר הדפים עם `image_provenance` עדיין נכשלים:

```bash
python3 scripts/deed_standard.py --failing 147   # כיתובים ארוכים מדי
python3 scripts/deed_standard.py --failing 20    # עברית שנשארת אחרי החלפת שפה
```

הדרך: גיליון לכל דף ב-`docs/enrichment/captions/<id>.json` →
`scripts/apply_captions.py <גיליון> --apply`, ותוויות המקורות דרך
`scripts/apply_citation_en.py`. התיאור הארוך של התמונה עובר ל-`caption_long_he`
ולא נמחק. **עובדי Opus, דף אחד לכל עובד** (כלל האיכות: אפס טעויות לפני מהירות).

**שורה אחת היא ברירת מחדל, לא גיליוטינה.** תומר חידד ב-11.8: תמונה שההקשר שלה
באמת דורש — ורק היא — נשארת ארוכה. המבחן: **בלי ההסבר לא מבינים למה התמונה בדף,
והטקסט לא נותן את ההקשר** (כלל 58). אז כותבים נימוק ב-`caption_why_long`, והכיתוב
מותר עד 200 תווים ו-2 משפטים (אנגלית 240), עד 2 כאלה בדף. בלאמאר יצא חריג אחד
מתוך תשעה — קבר-הכבוד בווינה. זה היחס שאליו לכוון.

---

# עתיד — מוסכם, לא עכשיו

- **להוסיף עוד ועוד מעשים לאורך כל ההיסטוריה.** זו לא משימה עם סוף — זו הכוונה
  הקבועה של האתר. 201 זו ההתחלה. כל החלטת תכנון צריכה להחזיק גם ב-1000 מעשים.
- **דפי אדם.** לכן `people[]` נאסף כבר עכשיו במעבר: לשלוף בעתיד את כל מה שאדם אחד
  עשה, בלי מעבר חוזר על 201 מעשים.
- **קו זמן** — ציר שנים אופקי שגוררים עליו, כל מעשה נקודה. **נדחה לאחרי התפאורה
  ואחרי הוספת המעשים**, ואז נחליט אם הוא מחליף את כפתורי התקופות (לא מתווסף עליהם).
- **תיוג יש-סרטון / אין-סרטון** ותיוג מדינות ספציפיות.

# נדחו במפורש

תג "היקף השפעה" (כפילות של "למי זה עזר") · תג "חוזק התיעוד" · תג פרסים והכרה
(מופיע בתוכן) · תג חקלאות/מים/טכנולוגיה (נכנס תחת המצאה מדעית) · פס "כך נבדק"
עם תאריכים על הדף (מכער) · קופסת "מה עדיין לא ברור" · דף "איך אנחנו מאמתים" ·
מעשה היום.
